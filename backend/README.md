# Saint Backend

The backend owns application logic, orchestration, goal understanding, context discovery, path generation, LLM integration, and DataHub integration boundaries.

Run locally from the repository root:

```powershell
uvicorn backend.app.main:app --reload
```

